name: islami_app
description: İslami mobil uygulama - MCP entegre
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.7
  
  # Modern UI & Animations
  shimmer: ^3.0.0
  lottie: ^3.1.0
  animate_do: ^3.3.4
  flutter_staggered_animations: ^1.1.1
  glassmorphism: ^3.0.0
  
  # State Management & Utilities
  provider: ^6.1.1
  intl: ^0.19.0
  
  # Sensors & Location
  flutter_compass: ^0.8.0
  geolocator: ^11.0.0
  permission_handler: ^11.3.0
  
  # Audio & Media
  audioplayers: ^6.0.0
  
  # Storage
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true